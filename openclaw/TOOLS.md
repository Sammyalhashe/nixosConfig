# TOOLS.md

# Available system tools
- `nix`: Provides the `nix` CLI for inspecting and building flakes as well as store paths
- `python`: Provides the `python` interpreter for running scripts and commands
- `podman`: Provides the `podman` CLI for managing containers, pods, and images
